ohysScheduleApplication:
  component: '@sls-next/serverless-component'
  inputs:
    build:
      memory: 1024
      env:
        apiUrl: ${env.apiUrl}
        apiSearchUrl: ${env.apiSearchUrl}
        googleAnalyticsTag: ${env.googleAnalyticsTag}
